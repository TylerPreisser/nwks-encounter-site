{
  "name": "nwks-encounter-site",
  "version": "0.1.0",
  "private": true,
  "description": "Recovered redesign concept for **nwksencounter.com** (Northwest Kansas Encounter — Men's Encounter / Women's Encounter). This is the \"Animated Gateway\" concept we built as a Claude Artifact and iterated on.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx wrangler pages dev dist --local",
    "deploy": "echo 'Refusing ambiguous deploy. Use: npm run deploy:worlds (front-end -> nwks-encounter-site) OR npm run deploy:backend (api+admin -> nwks-encounter-backend).' && exit 1",
    "build:worlds": "rm -rf dist-worlds && WORLDS_OUT=dist-worlds node build/bundle.mjs",
    "deploy:worlds": "npm run build:worlds && npx wrangler pages deploy dist-worlds --project-name nwks-encounter-site --branch main",
    "deploy:backend": "npm run build && npx wrangler pages deploy dist --project-name nwks-encounter-backend --branch main",
    "test": "vitest run --config vitest.config.ts && vitest run --config vitest.photos.config.ts",
    "test:api": "vitest run --config vitest.config.ts && vitest run --config vitest.photos.config.ts",
    "test:api:watch": "vitest --config vitest.config.ts",
    "test:admin": "vitest run --config admin/vitest.config.ts",
    "test:e2e": "playwright test",
    "deploy:email": "npx wrangler deploy --config email-worker/wrangler.toml",
    "db:migrate": "npx wrangler d1 migrations apply nwks-encounter",
    "db:migrate:local": "npx wrangler d1 migrations apply nwks-encounter --local"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20260702.1",
    "@simplewebauthn/browser": "^13.3.0",
    "@simplewebauthn/server": "^13.3.2",
    "hono": "^4.7.0",
    "jose": "^6.2.7",
    "jsdom": "^29.1.1",
    "playwright": "^1.61.1",
    "resend": "^4.7.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0",
    "wrangler": "4.35.0"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TylerPreisser/nwks-encounter-site.git"
  },
  "bugs": {
    "url": "https://github.com/TylerPreisser/nwks-encounter-site/issues"
  },
  "homepage": "https://github.com/TylerPreisser/nwks-encounter-site#readme",
  "dependencies": {
    "postal-mime": "^2.7.5"
  }
}
